#include "branches.hpp"

Branches :: Branches() {}
Branches :: Branches(
    double first_diam,
    double first_length)
    :   diam(first_diam),
        length(first_length),
        parent(nullptr)
{

}

void Branches :: AppendChild(
    double distance_from_last,
    double angle_from_parent)
{

}

void Branches :: AppendChild(
    double distance_from_last,
    double angle_from_parent,
    double first_diam,
    double first_length)
{
    
}